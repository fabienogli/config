[alias]
	# View abbreviated SHA, description, and history graph of the latest 20 commits
	l = log --pretty=oneline -n 20 --graph --abbrev-commit

[diff]
	tool = vscode
[difftool "vscode"]
	cmd = code --wait --diff $LOCAL $REMOTE

[user]
	name = fabienogli
	email = fabien.ogli@etu.univ-lyon1.fr
[core]
	autocrlf = input
